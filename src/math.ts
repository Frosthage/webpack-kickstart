
export function add(x: number, y: number): string {
    return (x + y).toString();
}

export function sub(x: number, y: number): string {
    return (x + y).toString();
}

export function mul(x: number, y: number): string {
    return (x + y).toString();
}


export function div(x: number, y: number): string {
    return (x + y).toString();
}